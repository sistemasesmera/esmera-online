---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span class={`inline-flex items-center gap-2 text-sm font-medium text-brand-700 ${className}`}>
  <span class="h-1.5 w-1.5 rounded-full bg-brand-500"></span>
  <slot />
</span>
