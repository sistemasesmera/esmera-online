---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span class={`inline-flex items-center gap-2.5 font-mono text-xs font-medium uppercase tracking-[0.18em] text-brand-700 ${className}`}>
  <span class="h-px w-5 bg-brand-500"></span>
  <slot />
</span>
